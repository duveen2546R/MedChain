MEDCHAIN_CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
MEDCHAIN_SECRET_KEY=replace-with-at-least-32-random-characters
MEDCHAIN_ACCESS_TOKEN_MINUTES=120
MEDCHAIN_REFRESH_TOKEN_DAYS=14
MEDCHAIN_INVITATION_EXPIRES_DAYS=7
MEDCHAIN_RESET_TOKEN_MINUTES=60
MEDCHAIN_MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-host>/?retryWrites=true&w=majority
MEDCHAIN_MONGODB_NAME=medchain
AZURE_STORAGE_CONNECTION_STRING=
AZURE_STORAGE_ACCOUNT_URL=https://<storage-account>.blob.core.windows.net
AZURE_STORAGE_CONTAINER=medchain-artifacts
# Embedded consortium blockchain. Both values are optional:
# MEDCHAIN_CHAIN_ID defaults to 7777; MEDCHAIN_SIGNER_PRIVATE_KEY is derived from
# MEDCHAIN_SECRET_KEY when unset. Keep whichever is used stable, because the
# stored chain rejects blocks from a different authority key.
MEDCHAIN_CHAIN_ID=7777
MEDCHAIN_SIGNER_PRIVATE_KEY=
MEDCHAIN_BOOTSTRAP_ADMIN_EMAIL=
MEDCHAIN_BOOTSTRAP_ADMIN_PASSWORD=
MEDCHAIN_BOOTSTRAP_ADMIN_NAME=Platform Administrator
# Transactional email via Brevo (https://www.brevo.com). Optional: when unset, invitation
# and password-reset links are logged server-side and returned to the inviting admin in the
# API response, so the invite-only flow works without an email provider. MEDCHAIN_MAIL_FROM_EMAIL
# is required whenever MEDCHAIN_BREVO_API_KEY is set. MEDCHAIN_FRONTEND_BASE_URL builds the
# links emailed to users (e.g. {base}/register?token=...).
MEDCHAIN_BREVO_API_KEY=
MEDCHAIN_MAIL_FROM_EMAIL=
MEDCHAIN_MAIL_FROM_NAME=MedChain
MEDCHAIN_FRONTEND_BASE_URL=http://localhost:5173
